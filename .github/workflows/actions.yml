name: OpenAPI Checks

on:
  - push

jobs:
  build:
    name: Linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: stoplightio/spectral-action@latest
      - name: Archive Report
        uses: actions/upload-artifact@v3
        with:
          name: openapi-linting-report
          path: report.txt
